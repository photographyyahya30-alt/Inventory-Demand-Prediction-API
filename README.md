# Inventory-Demand-Prediction-API
An end-to-end machine learning system for predicting inventory demand using historical retail data. The project includes a trained XGBoost model, a FastAPI backend for inference, and a Streamlit web application for user interaction.
